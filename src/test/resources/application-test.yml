spring:

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true

server:
  port: 9876

logging:
  level:
    com.com.stahovskyi: DEBUG



